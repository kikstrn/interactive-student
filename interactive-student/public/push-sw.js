/* KLIKAO - Service Worker Web Push V3 */
self.addEventListener("push",event=>{let p={title:"KLIKAO",body:"Nouvelle notification",url:"/admin"};try{if(event.data)p={...p,...event.data.json()}}catch(e){console.error("KLIKAO PUSH SW payload error",e)}event.waitUntil(self.registration.showNotification(p.title,{body:p.body,icon:"/icon.png",badge:"/icon.png",tag:p.tag||(p.ticketNumber?`klikao-ticket-${p.ticketNumber}`:"klikao-admin"),renotify:true,data:{url:p.url||"/admin"}}))});
self.addEventListener("notificationclick",event=>{event.notification.close();const target=event.notification.data?.url||"/admin";event.waitUntil(clients.matchAll({type:"window",includeUncontrolled:true}).then(async windows=>{for(const client of windows){if("navigate" in client)await client.navigate(target);if("focus" in client)return client.focus()}return clients.openWindow?clients.openWindow(target):undefined}))});
